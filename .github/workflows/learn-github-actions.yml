name: Run SQL test

on: [push]

jobs:

  runner-job:

    runs-on: ubuntu-latest

    services:

      image: gvenzl/oracle-xe:11-slim

      env:
        ports:
          - 1521:1521
    steps:
      - uses: actions/checkout@v2
      - run: docker ps -a
